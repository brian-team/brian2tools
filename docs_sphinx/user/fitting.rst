Model fitting
=============

The `brian2tools` package offers tools to fit models to data, such as current traces recorded in voltage-clamp.

To fit a model to a set of experimental traces, use `brian2tools.modelfitting.fit_traces`.
